# m3u8 去广告：PTS 时间轴法

> 适用范围：本项目「先下后播」下载管线（`app/src/main/java/com/tv/mailvod/download/AdDetector.kt` + `M3u8Downloader.kt`），v0.8.0 起为唯一广告检测算法。本文完整描述其原理、步骤、护栏与实测数据，可供其它项目复刻。

## 1. 背景与问题

免费影视资源站（量子 `lz-cdn*.com`、非凡 `ffzy-plays.com` 等）的 m3u8 是「混流清单」：

- 子清单固定名为 `mixed.m3u8`，正片被切成若干组，每组约 20~40 秒，组与组之间有 `#EXT-X-DISCONTINUITY` 标记；
- 广告以**整组**硬拼接在正片组之间，两站实测单块 17~30 秒（1~2 组）。

检测难点：广告分片与正片分片的编码参数**完全一致**（同分辨率、同帧率、同编码器、同 TS 流 PID），无法从元数据区分；文件名在非凡源是哈希串、在量子源是递增数字，也没有统一特征。

两站实为同一套混流插广告管线（同一广告重复拼接时，两处 PTS 完全相同），因此一套算法通用。

## 2. 核心原理

混流管线拼接广告时**不重排时间戳**，于是时间轴上留下两种可测量的痕迹：

1. **正片组的 PTS 链严格连续**：每组首个分片的 PTS（展示时间戳，90 kHz 时钟）等于前面所有正片组的 EXTINF 时长之和加一个固定偏移（两站实测偏移约 1.44~1.48 秒，整片累计漂移仅 0.1 秒）；
2. **广告组的 PTS 脱轨**：广告是独立小文件，其 PTS 与清单累计时长对不上（块首常为 1.5 秒左右的新文件起点）；且广告组之后的正片组会**恢复**预期时间轴——「脱轨 + 恢复」是判定广告的决定性证据。

因此检测方法为：解析每组首分片的 PTS，与「清单累计时长 − 已判定广告时长」对比，命中（容差 2 秒）判正片，脱轨判广告。

## 3. 检测步骤

1. **解析清单**：按 `#EXT-X-DISCONTINUITY` 分组（注意排除 `#EXT-X-DISCONTINUITY-SEQUENCE`），记录每分片的 `#EXTINF` 时长。组数 < 2 则不适用（无分组无广告可剔）。
2. **并发抓头**：8 线程对每组首个分片发 HTTP Range 请求（`bytes=0-65535`），只取头部 64 KB——PTS 一定在分片头部；服务器不支持 Range 时读到 64 KB 即断开。单组失败重试 3 次。
3. **解析 PTS**：纯字节解析，无需任何多媒体库——
   - 按 188 字节扫描 MPEG-TS 包（首字节 0x47 同步字，失配逐字节重找）；
   - 取 PUSI=1 的包，跳过自适应字段，校验 PES 起始码 `00 00 01`；
   - PES 标志字节最高位为 1 表示含 PTS，按 33 位格式解析后除以 90000 得秒。
4. **分类**：组 0 定义时间轴偏移（组 0 扫描失败则放弃检测）；逐组推进预期起点，命中判正片、脱轨判广告。扫描失败的组按正片保留（宁多勿缺），其后的组命中时用双假设消歧（把失败组当作正片或广告两种预期都试，命中哪个按哪个回溯标记）。
5. **护栏校验**（见 §4）。
6. **剔除与拼接**：删除广告组的全部分片，其余分片按序下载后直接二进制拼接为单一 TS 文件。正片时间戳天然连续，拼接处毫秒级对齐，无需任何时间戳处理，也不存在播放清单标记问题。

## 4. 护栏（防误判的关键）

任何一项超限即视为误判，**整体放弃剔除、按原清单下载**（宁可带广告，不丢正片）：

| 护栏 | 阈值 | 依据 |
|---|---|---|
| 广告块数 | ≤ 5 块 | 实测每片 3 块 |
| 单块大小 | ≤ 4 组且 ≤ 120 秒 | 实测单块 1~2 组、17~30 秒 |
| 广告总占比 | ≤ 15% | 实测约 1~1.7% |

护栏专门防御「正片时间戳整体错位」类源（例如正片本身由多段独立编码拼接，PTS 中途重置）：这类源的脱轨会连环扩散，最终表现为广告块数或占比超限而被整体放弃，不会乱删正片。

## 5. 缓存

检测结果按清单内容 SHA-256 指纹缓存于 `movies/adcache/ad_<指纹前16位>.json`（内容为广告组号数组）。同清单重新下载时免扫描；清单内容变化（指纹变）自动失效。空结果（无广告）同样缓存，避免重复扫描。

## 6. 实测数据

| 源 | 影片 | 时长 | 分组 | 广告 | 判定一致性 |
|---|---|---|---|---|---|
| 量子 lz-cdn3 | 末日激战 | 117.2 分钟 | 180 组 | 3 块 × 26 秒 = 78 秒（组 8/52/99，各 7 段） | PTS 法与文件名跳变法**完全一致** |
| 非凡 ffzy | 25869_e69dfbe9 | 75.8 分钟 | 186 组 | 3 块共 56.6 秒（组 13-14/70-71/152-153） | 块 1 与块 3 为同一广告（PTS 完全相同），零误判 |

两片正片链偏移全程恒定（1.44~1.48 秒），整片漂移 0.1 秒；扫描 366 组头部请求零失败。

## 7. 边界与已知限制

- **仅作用于「先下后播」**：在线播放不去广告（ExoPlayer 在线流去广告需改写播放清单或本地代理，复杂度高，未做）。
- **依赖 `#EXT-X-DISCONTINUITY` 分组**：无该标记的清单不检测。
- **AES-128 加密源**：分片头部是密文，解析不出 PTS → 自动放弃检测；正常下载与解密不受影响。
- **片头第一组即广告**的情形无法与正片区分（时间轴从 0 开始存在歧义，会把两者都判为正片）；实测两站广告均不在片头。

## 8. 复刻要点

- 无需 ffmpeg 或第三方多媒体库，PTS 解析约 60 行纯字节代码；
- Range 请求被服务器忽略也无妨，读到 64 KB 即断开即可；
- 一定要做护栏：分类器只负责测量，护栏负责兜底决策，两者分离才敢在真实源上自动删除数据；
- 成片直接输出 MPEG-TS（ExoPlayer 等播放器原生支持），避免重封装。
